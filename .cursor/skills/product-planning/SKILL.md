---
name: product-planning
description: Create PRDs, roadmaps, user stories, and MVP scope for the active product.
---

# Product Planning

Use `active_product_id` from `workspace/context/workspace-state.json`, then read the product row in `product-registry.md`.

Outputs should be concise and saved under `outputs/product/` when requested.
